/**
 * Fixture: GET /api/v1/cards/{cardId}
 * Scenario: API returns an error when fetching card details
 */
export const errorResponse = {
  errorCode: 'FETCH_ERROR',
  errorMessage: 'Failed to fetch card details',
};
