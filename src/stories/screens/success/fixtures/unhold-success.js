export const unholdSuccess = {
  // Endpoint: n/a — state passed from feature-flow after POST /api/v1/cards/{cardId}/unhold
  // Description: Confirmation data after card is reactivated
  transactionType: 'unheld',
};
