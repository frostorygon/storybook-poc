export const holdSuccess = {
  // Endpoint: n/a — state passed from feature-flow after POST /api/v1/cards/{cardId}/hold
  // Description: Confirmation data after card is put on hold
  transactionType: 'held',
};
