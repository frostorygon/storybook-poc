export const sessionExpiredError = {
  // Endpoint: n/a — 401 response
  // Description: Auth session expired
  errorCode: 'SESSION_EXPIRED',
  errorTitle: 'Session expired',
  errorMessage: 'Your session has expired. Please log in again.',
  retryable: false,
};
